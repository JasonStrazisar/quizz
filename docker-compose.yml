version: "3.8"
services:
  quizrush:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./server/data:/app/server/data
    environment:
      - JWT_SECRET=change-me
      - ADMIN_PASSWORD=change-me
    restart: unless-stopped
